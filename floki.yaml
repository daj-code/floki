image: ekidd/rust-musl-builder
mount: /home/rust/src
forward_ssh_agent: true
docker_switches:
  # Pass through the user's RUST_BACKTRACE setting if available.
  - -e
  - RUST_BACKTRACE
shell: bash
# Some of the floki tests expect to be able to use docker commands. For this to
# work, we need "dind: true" and the docker CLI tools installed in the
# container.
# dind: true
# init:
#   # Install docker - see https://docs.docker.com/engine/install/ubuntu/
#   - 'sudo apt-get update &&
#     sudo apt-get install -y apt-transport-https ca-certificates curl gnupg-agent software-properties-common &&
#     curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add - &&
#     sudo add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" &&
#     sudo apt-get autoremove -y software-properties-common &&
#     sudo apt-get update &&
#     sudo apt-get install -y docker-ce docker-ce-cli containerd.io &&
#     sudo usermod -aG docker $(whoami) &&
#     sudo apt-get clean && sudo rm -rf /var/lib/apt/lists/*'
